---
phase: 01-audit-and-harden-scaffold
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - HELD_OUT.md
autonomous: false
requirements:
  - BENCH-01
  - BENCH-02
  - BENCH-03
must_haves:
  truths:
    - "A committed HELD_OUT.md at repo root records the held-out-fixture decision before Phase 2 freeze"
    - "The decision is either (A) no holdout with written rationale or (B) explicit holdout with named task ids"
    - "The file is referenced in git log so future readers can verify it preceded the freeze tag"
  artifacts:
    - path: "HELD_OUT.md"
      provides: "Methodology decision record for held-out fixtures"
      min_lines: 15
  key_links:
    - from: "HELD_OUT.md"
      to: "Phase 2 freeze tag"
      via: "file committed BEFORE harnesses-frozen tag is set"
      pattern: "Decision|Option A|Option B"
---

<objective>
Create and commit `HELD_OUT.md` at the repo root recording the explicit held-out-fixture decision. This is the Phase 2 freeze gate: without this file committed, Phase 2 cannot proceed because the freeze would encode an unstated fixture policy.

Purpose: With only 5 fixtures total, holding 2 out is statistically tight but methodologically stronger; the alternative is an honest "no holdout, rationale: N=5 too small" — both are defensible; silence is not. The article's reproducibility claims require the decision be in git history before any matrix numbers are published.

This plan has a human-decision checkpoint: Claude cannot pick between Option A (no holdout) and Option B (retroactive holdout) because it is a methodological judgment call — not a technical one. Claude drafts both branches; the user selects.

Output:
- `HELD_OUT.md` at repo root with one branch selected and rationale filled in
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-and-harden-scaffold/01-RESEARCH.md
@.planning/research/PITFALLS.md
@src/harness_eng/tasks/tasks.jsonl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft HELD_OUT.md with both option branches + current-state scan</name>
  <files>HELD_OUT.md</files>
  <action>
    Before drafting, read `src/harness_eng/tasks/tasks.jsonl` to get the exact task IDs of the 5 fixtures (likely `task_01` ... `task_05` or similar). Also scan `src/harness_eng/harnesses/*.py` and `src/harness_eng/tools.py` for any direct reference to specific task IDs (via `grep -rn "task_0" src/`). Record findings in the draft.

    Write `HELD_OUT.md` at repo root with the following content (fill in task IDs from the scan; leave both option branches intact for the user to choose at the checkpoint):

    ```markdown
    # Held-Out Fixtures Decision

    **Decided:** 2026-04-23 (before Phase 2 freeze)
    **Status:** DRAFT — awaiting user selection between Option A and Option B at checkpoint.
    **Context:** harness_eng v1 benchmark uses 5 HTML extraction fixtures across 5 domains. Phase 2 tags a git commit as `harnesses-frozen`; the freeze encodes whatever holdout policy is in place at that moment. This file records the explicit choice so the article can cite it.

    ## Fixtures on disk

    Fixtures under `src/harness_eng/tasks/fixtures/`, registered in `src/harness_eng/tasks/tasks.jsonl`:

    <!-- Fill in from tasks.jsonl scan — one bullet per task id -->
    - task_01 — <domain from jsonl>
    - task_02 — <domain from jsonl>
    - task_03 — <domain from jsonl>
    - task_04 — <domain from jsonl>
    - task_05 — <domain from jsonl>

    ## Current developer visibility (honest assessment)

    <!-- Fill in: were any task IDs hard-referenced in committed harness/tool code?
         Result of `grep -rn "task_0" src/` -->

    Harness prompts, tool code, and grader code were committed without any task-id reference (verified by grep). However, the harnesses were designed with knowledge of the fixture domain mix (product, job posting, event, recipe, paper metadata) and of the presence of decoys. A clean holdout in the strict sense ("model author never saw the fixture") is NOT claimed — see Option A rationale.

    ---

    ## Option A: No holdout (recommended for v1)

    **Choice:** All 5 fixtures are used for both pilot and matrix. No task is designated held-out.

    **Rationale:**
    With 5 fixtures total, holding 2 out leaves 3 for harness iteration. Given that harness prompts were already written against the full known domain mix (product, job, event, recipe, paper) and that decoys were authored knowing the fixtures, the "clean holdout" property is already weak. Rather than claim a holdout that is structurally compromised, v1 uses all 5 fixtures transparently. The article will state: "All 5 fixtures were visible during harness development. v2 (SCALE-02) introduces a 2-fixture holdout against an expanded 40-fixture set."

    **Consequence:** Article section "Limitations" must state: "No held-out fixtures in v1; effect size measured on dev set. v2 plans 2 held out of 40."

    ---

    ## Option B: Retroactive holdout

    **Choice:** `task_04` and `task_05` are designated held-out. From this commit forward, any change to `src/harness_eng/harnesses/*.py`, `src/harness_eng/tools.py`, or `src/harness_eng/model.py` that references `task_04` or `task_05` by id invalidates the holdout.

    **Rationale:**
    Even with a weak holdout, reporting separate numbers for "dev set (task_01..03)" vs "held-out (task_04..05)" flags any harness that overfit to the dev set. The article can honestly report both numbers.

    **Enforcement:** Phase 1 adds a pytest (NOT in this plan — tracked as follow-up) that greps harness/tool source for `task_04` or `task_05` references. If found, test fails.

    **Consequence:** Matrix analysis splits reporting into dev-set and held-out columns.

    ---

    ## Rationale (final selection)

    <!-- FILLED IN BY USER AT CHECKPOINT -->

    [Selected option: A or B]

    [One-paragraph justification for the selection — why this choice fits the experiment's reproducibility and statistical-power profile.]
    ```

    Rules:
    - Do NOT commit the file with `<!-- FILLED IN BY USER AT CHECKPOINT -->` placeholders intact. The checkpoint (Task 2) produces the final content before commit.
    - Do NOT add the pytest enforcement from Option B in this plan. It is a follow-up if Option B is chosen, tracked as a new plan.
    - Fill in the actual task_id/domain list from the tasks.jsonl scan.
    - Preserve BOTH option branches in the file — the article will reference the unchosen branch as "considered and rejected" context.
  </action>
  <verify>
    `test -f HELD_OUT.md` — exists.
    `grep -n "Option A" HELD_OUT.md && grep -n "Option B" HELD_OUT.md` — both branches present.
    `grep -n "task_0" HELD_OUT.md` — task IDs enumerated from tasks.jsonl.
  </verify>
  <done>
    `HELD_OUT.md` drafted at repo root with both option branches present and task IDs enumerated from the live tasks.jsonl. Final selection section still placeholder pending checkpoint.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: User selects Option A or Option B</name>
  <files>HELD_OUT.md</files>
  <action>
    Pause execution. Present the decision + options below to the user. Do NOT pick on behalf of the user — the choice between Option A (no holdout) and Option B (retroactive holdout of task_04 + task_05) is a methodological judgment (sample-size, honesty-of-clean-holdout-claim, article narrative). Wait for the user to respond with the resume-signal. Then proceed to Task 3.
  </action>
  <decision>Held-out fixtures policy for v1 matrix run</decision>
  <context>
    Only 5 fixtures exist. Phase 2 freeze tag will encode whatever policy is active. Two defensible options are drafted in `HELD_OUT.md`. Claude cannot pick: the decision is a methodological judgment (sample size, honesty of "clean holdout" claim, article narrative). Pick one, then Claude finalizes the file and commits.
  </context>
  <options>
    <option id="option-a">
      <name>Option A: No holdout</name>
      <pros>
        - Maximizes statistical power (all 5 tasks, all 3+ seeds = 15+ trials per harness instead of 9)
        - Intellectually honest: "holdout" with n=2 held from n=5 total is a weak claim anyway
        - Simplest to explain in article methodology
      </pros>
      <cons>
        - Limitations section must explicitly flag no-holdout
        - No internal signal of harness overfitting to dev set
      </cons>
    </option>
    <option id="option-b">
      <name>Option B: task_04 + task_05 held out</name>
      <pros>
        - Article can report separate dev-set vs held-out numbers
        - Any harness overfitting dev set gets flagged by spread between the two
        - Stronger-looking methodology claim
      </pros>
      <cons>
        - Statistical power on the holdout is very low (n=2 tasks)
        - Holdout "cleanness" is already compromised because harness prompts were written knowing the domain mix
        - Requires follow-up enforcement pytest (not in this plan)
      </cons>
    </option>
  </options>
  <verify>User has typed "option-a" or "option-b" (or given equivalent clear signal).</verify>
  <done>User selection recorded; Task 3 can proceed to finalize HELD_OUT.md.</done>
  <resume-signal>Select: option-a or option-b. After selection, Claude finalizes HELD_OUT.md (deletes the unchosen branch's "Choice" from the Rationale section, fills in the one-paragraph justification, removes the DRAFT status header) and commits.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize HELD_OUT.md with the chosen option</name>
  <files>HELD_OUT.md</files>
  <action>
    Based on the user's selection at the checkpoint:
    1. Update the `**Status:**` line to remove "DRAFT — awaiting user selection" and replace with `**Status:** FINAL — selected Option [A|B]`.
    2. Keep BOTH option branches in the file as context — they are the considered-and-rejected record.
    3. Fill in the "Rationale (final selection)" section at the bottom with the one-paragraph justification per user's resume-signal reasoning (or write a conservative one-paragraph justification citing the chosen option's pros from the checkpoint).
    4. If Option B was selected, add a brief FOLLOW-UP note at the end: "Follow-up plan required: add pytest greppping harness/tool source for task_04/task_05 references; failure invalidates holdout."

    Do NOT commit in this task — git commit is handled by the orchestrator's summary step after the plan completes.
  </action>
  <verify>
    `grep -n "Status:" HELD_OUT.md | head -1` shows `FINAL`, not `DRAFT`.
    `grep -n "Rationale (final selection)" HELD_OUT.md` — non-empty section with real prose below it (not `<!-- ... -->`).
    `grep -c "Option A\\|Option B" HELD_OUT.md` — returns 2 or more (both branches preserved).
  </verify>
  <done>
    `HELD_OUT.md` is FINAL, documents the chosen option and the rejected one, and is ready for commit.
  </done>
</task>

</tasks>

<verification>
- `test -f HELD_OUT.md && wc -l HELD_OUT.md` — file exists, >= 15 lines
- `grep -n "Status:.*FINAL" HELD_OUT.md` — non-empty
- `grep -n "Option A\\|Option B" HELD_OUT.md` — both branches preserved
- `grep -n "DRAFT\\|FILLED IN BY USER\\|<!-- " HELD_OUT.md` — no remaining placeholder markers
</verification>

<success_criteria>
1. `HELD_OUT.md` exists at repo root (not under `.planning/` or `docs/`).
2. Status is FINAL; user has selected Option A or Option B.
3. Task IDs enumerated correctly from the live tasks.jsonl.
4. Both option branches preserved in the file as considered-and-rejected record.
5. One-paragraph justification filled in.
6. File is committable (no TODOs, no placeholders).
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-and-harden-scaffold/01-04-SUMMARY.md` summarizing:
- Selected option (A or B) and one-sentence why
- Task-id list at time of decision
- Requirement IDs addressed (BENCH-01, BENCH-02, BENCH-03 — fixture-scope documentation)
- Follow-up required if Option B was chosen
</output>
