---
phase: 01-audit-and-harden-scaffold
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - .planning/phases/01-audit-and-harden-scaffold/AUDIT.md
autonomous: true
requirements:
  - HARN-01
  - HARN-02
  - HARN-03
  - HARN-04
  - HARN-05
  - HARN-06
  - RUN-01
  - RUN-03
  - RUN-05
  - RUN-06
  - ANAL-01
  - ANAL-03
  - ANAL-04
  - ART-01
  - ART-02
  - VIEW-01
  - VIEW-02
  - VIEW-03
  - VIEW-04
  - ONB-03

must_haves:
  truths:
    - "Every one of the 34 Phase 1 requirement IDs maps to an existing test, newly-added test, or documented manual evidence"
    - "The full test suite (existing + new) runs green after all Wave 1 deltas land"
    - "AUDIT.md lists the requirement-to-evidence map for future readers and gates Phase 2"
    - "No regression introduced by Wave 1 deltas — existing behaviors still pass"
  artifacts:
    - path: ".planning/phases/01-audit-and-harden-scaffold/AUDIT.md"
      provides: "Requirement-to-test traceability matrix for all 34 Phase 1 requirement IDs"
      min_lines: 80
  key_links:
    - from: "AUDIT.md"
      to: "each Phase 1 requirement ID"
      via: "table row with test file + test name (or manual-verified evidence path)"
      pattern: "HARN-|INTG-|TRACE-|BENCH-|RUN-|ANAL-|ART-|VIEW-|TEST-|ONB-"
    - from: "AUDIT.md"
      to: "pytest suite green"
      via: "`pytest tests/ -v` output snippet or pass count"
      pattern: "passed"
---

<objective>
Audit-pass validation: produce the requirement-to-evidence map proving all 34 Phase 1 requirement IDs are satisfied after Wave 1's four plans land. This is the Phase 2 gate — Phase 2 cannot freeze until the audit matrix shows every Phase 1 requirement has concrete evidence (test name, or manual-check evidence).

Purpose: The article's credibility rests on "we built this matrix and every structural invariant is test-pinned." Without an audit record, reviewers cannot verify which requirements are genuinely test-pinned vs merely claimed. AUDIT.md closes that gap.

This plan is Wave 2 — runs ONLY after plans 01-01, 01-02, 01-03, and 01-04 have all landed. It does not modify source code; it documents state.

Output:
- `.planning/phases/01-audit-and-harden-scaffold/AUDIT.md` — a table mapping each of the 34 requirement IDs to its evidence (test file::test_name, or manual-verified artifact path with 1-sentence rationale).
</objective>

<execution_context>
@C:/Users/Utilisateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Utilisateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-audit-and-harden-scaffold/01-RESEARCH.md
@.planning/phases/01-audit-and-harden-scaffold/01-01-SUMMARY.md
@.planning/phases/01-audit-and-harden-scaffold/01-02-SUMMARY.md
@.planning/phases/01-audit-and-harden-scaffold/01-03-SUMMARY.md
@.planning/phases/01-audit-and-harden-scaffold/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and capture green-state evidence</name>
  <files>.planning/phases/01-audit-and-harden-scaffold/AUDIT.md</files>
  <action>
    Run the full test suite and capture the output verbatim for the audit record. Concrete commands:

    ```bash
    pytest tests/ -v --tb=short 2>&1 | tee /tmp/phase1_audit_pytest.log
    ```

    Extract:
    - Total test count (was 19 pre-phase; should be 24+ post-phase after Wave 1 adds test_trace_schema.py (2), test_grader_determinism.py (2), test_model_usage.py (2), test_tool_result_rebilling.py (1) = 7 new)
    - Pass count (all must pass — zero tolerance for failures or skips in the audit)
    - Runtime (informational)
    - New test file names that appear in the output (sanity-check Wave 1 actually added them)

    If ANY test fails or errors: do NOT proceed to Task 2. Stop and report failure back. The audit cannot claim green if it isn't.

    Also run these greps as evidence:
    ```bash
    grep -n "SCHEMA_VERSION" src/harness_eng/trace.py
    grep -n "os.fsync" src/harness_eng/trace.py
    grep -n "max_retries=0" src/harness_eng/model.py
    grep -n "usage_raw" src/harness_eng/model.py
    grep -n "unicodedata.normalize" src/harness_eng/grader.py
    grep -nr "\.lower()" src/harness_eng/grader.py     # MUST return no matches
    test -f HELD_OUT.md && echo "HELD_OUT.md present"
    ```

    Record the output of each check. These are the "structural invariant" evidence for AUDIT.md's requirement table.

    Do NOT write AUDIT.md in this task — Task 2 does the write after all evidence is collected.
  </action>
  <verify>
    `pytest tests/ -q` exits with zero failures.
    Every grep above returns the expected shape (non-empty for positive checks, empty for the `.lower()` negative check).
  </verify>
  <done>
    Test suite is green; all structural-invariant greps return their expected results; evidence captured for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write AUDIT.md — requirement-to-evidence matrix for all 34 IDs</name>
  <files>.planning/phases/01-audit-and-harden-scaffold/AUDIT.md</files>
  <action>
    Write `.planning/phases/01-audit-and-harden-scaffold/AUDIT.md`. Structure:

    ```markdown
    # Phase 1 Audit: Requirement-to-Evidence Matrix

    **Audited:** <today's date>
    **Phase:** 01-audit-and-harden-scaffold
    **Status:** GREEN — all 34 Phase 1 requirement IDs have concrete evidence.
    **Next gate:** Phase 2 (Methodology Freeze) — safe to proceed.

    ## Test suite summary

    - Total tests: <N> (was 19 pre-phase, +7 added in Wave 1)
    - Pass: <N>
    - Fail: 0
    - Skip: 0
    - Runtime: <seconds>

    ## Structural invariants (grep evidence)

    | Invariant | Check | Result |
    |-----------|-------|--------|
    | trace.py schema_version constant | `grep SCHEMA_VERSION src/harness_eng/trace.py` | present |
    | trace.py fsync per record | `grep os.fsync src/harness_eng/trace.py` | present |
    | model.py max_retries=0 | `grep max_retries=0 src/harness_eng/model.py` | present |
    | model.py usage_raw field | `grep usage_raw src/harness_eng/model.py` | present |
    | grader.py NFC normalization | `grep unicodedata.normalize src/harness_eng/grader.py` | present |
    | grader.py no .lower() | `grep "\\.lower()" src/harness_eng/grader.py` | ABSENT (correct) |
    | HELD_OUT.md at repo root | `test -f HELD_OUT.md` | present |

    ## Requirement-to-evidence matrix

    | ID | Description (short) | Evidence | Notes |
    |----|---------------------|----------|-------|
    | INTG-01 | Only model.py imports anthropic | tests/test_model_seal.py::test_only_model_py_imports_anthropic | AST-enforced (pre-existing) |
    | INTG-02 | Every harness routes through model.call | tests/test_harness_registry.py::test_no_harness_imports_anthropic_directly | Implicit via seal (pre-existing) |
    | INTG-03 | Model id, temp, max_tokens in config.py | Manual: `grep MODEL_ID src/harness_eng/config.py` | CONFIG singleton consumed everywhere |
    | HARN-01 | single_shot harness exists | tests/test_harness_registry.py (registry contains 'single_shot') | Plan 01-03 did not modify |
    | HARN-02 | react harness exists | tests/test_harness_registry.py | Plan 01-03 did not modify |
    | HARN-03 | plan_execute harness exists | tests/test_harness_registry.py | Plan 01-03 did not modify |
    | HARN-04 | reflexion harness exists | tests/test_harness_registry.py | Plan 01-03 did not modify |
    | HARN-05 | minimal harness exists | tests/test_harness_registry.py | Plan 01-03 did not modify |
    | HARN-06 | Every harness terminates via submit_answer | Manual: inspect harnesses/*.py for submit_answer call; tests/test_tools.py covers tool dispatch | |
    | BENCH-01 | 5 fixtures across 5 domains | tests/test_tasks.py::test_tasks_load | |
    | BENCH-02 | 3-5 expected fields per task | tests/test_tasks.py::test_expected_field_counts | |
    | BENCH-03 | Decoys in fixtures | Manual: fixtures/*.html contain sidebars/alt-prices; HELD_OUT.md references the fixture set | |
    | BENCH-04 | Grader does NFC + casefold + ASCII-ws collapse | tests/test_grader_determinism.py::test_golden_cases, test_100x_determinism | Wave 1 Plan 01-02 |
    | TRACE-01 | Event written before function returns | Implicit in trace.py; tests/test_trace_schema.py exercises log() | |
    | TRACE-02 | Line buffering + flush per event | Grep: buffering=1, os.fsync in trace.py | Wave 1 Plan 01-01 |
    | TRACE-03 | schema_version field | tests/test_trace_schema.py::test_every_record_has_schema_version | Wave 1 Plan 01-01 |
    | TRACE-04 | run_start + run_end bracket | tests/test_trace_schema.py::test_every_record_has_schema_version (exercises bracket) | |
    | TRACE-05 | ts + event_type + payload | tests/test_trace_schema.py (asserts 'ts' and 'type' on every record) | |
    | RUN-01 | CLI runs matrix sequentially | Manual: runner.py::run_matrix is sequential (grep for async/await returns nothing) | Smoke test deferred; low risk |
    | RUN-03 | JSONL row per cell | Manual: runner.py::_result_row writes one dict per cell | Existing |
    | RUN-05 | Cost estimator runs before full matrix | tests/test_cost_estimator.py::test_estimate_shape | |
    | RUN-06 | --yes confirmation | Manual: scripts/run_full.py shows the prompt | |
    | ANAL-01 | summary.csv produced | Manual: analysis.py writes summary.csv | Downstream; no test required in Phase 1 |
    | ANAL-03 | frontier.png produced | Manual: analysis.py writes frontier.png | Downstream |
    | ANAL-04 | field_heatmap.png produced | Manual: analysis.py writes heatmap | Downstream |
    | ART-01 | Article auto-drafted from CSV | Manual: analysis.py article generator | |
    | ART-02 | Numbers interpolated, not hand-typed | Manual: review article.md template | |
    | VIEW-01 | Single standalone HTML | Manual: trace_viewer.py emits one file | |
    | VIEW-02 | JSONL files rendered as collapsible | Manual: open trace_viewer.html in browser | |
    | VIEW-03 | Filterable by harness + task | Manual: trace_viewer.py inline dropdowns | |
    | VIEW-04 | Per-event type/fields/raw-JSON view | Manual: trace_viewer.py event renderer | |
    | TEST-01 | pytest suite covering grader, tasks, tools, seal, registry, cost | tests/test_grader.py, test_grader_determinism.py, test_tasks.py, test_tools.py, test_model_seal.py, test_harness_registry.py, test_cost_estimator.py, test_model_usage.py | Wave 1 added determinism + usage tests |
    | TEST-02 | Trace-schema regression + tool-result re-billing | tests/test_trace_schema.py, tests/test_tool_result_rebilling.py | Wave 1 Plans 01-01, 01-03 |
    | ONB-03 | .env.example shipped, no secrets | Manual: `test -f .env.example && grep -v "sk-ant-" .env.example` | |

    ## Post-audit open items (tracked to later phases, not blockers)

    - RUN-02 (--seeds flag), ANAL-02 (Wilson CIs), ANAL-05 (cost-per-success) — Phase 3
    - INTG-04, INTG-05, INTG-06 (freeze tag + pre-registration) — Phase 2
    - HARN-07, RUN-04 (tool allowlist assertion + manifests) — Phase 4
    - ONB-02 (hypothesis paragraph in README) — Phase 2
    - BENCH-05, TEST-03, ONB-01 — Phase 7

    ## Gate decision

    Phase 1 exits GREEN. Phase 2 may proceed:
    1. Confirm HELD_OUT.md is committed before Phase 2 freeze tag.
    2. Confirm pytest suite green at the commit that will receive the `harnesses-frozen` tag.
    3. Verify no `src/harness_eng/harnesses/*.py`, `tools.py`, or `model.py` edits since this audit commit (the freeze must encode the audited state, not a drifted one).
    ```

    Rules:
    - Fill the "Test suite summary" numbers from the actual pytest output captured in Task 1.
    - Every row in the main requirement table MUST reference either a concrete `tests/*.py::test_name` OR a `Manual:` check with enough detail for a reviewer to reproduce.
    - Do NOT silently change requirement wording from REQUIREMENTS.md. If the short description in the table differs materially, flag it in the Notes column.
    - Every one of the 34 IDs in this plan's `requirements` frontmatter MUST appear in the table. Cross-reference: {INTG-01..03, HARN-01..06, BENCH-01..04, TRACE-01..05, RUN-01, RUN-03, RUN-05, RUN-06, ANAL-01, ANAL-03, ANAL-04, ART-01, ART-02, VIEW-01..04, TEST-01, TEST-02, ONB-03}.
    - If any requirement cannot be pinned to evidence (neither a test nor a manual check), STATUS must become `AMBER` not `GREEN`, and the Gate decision section must block Phase 2 until that requirement is pinned.
  </action>
  <verify>
    `test -f .planning/phases/01-audit-and-harden-scaffold/AUDIT.md` — file exists.
    `grep -c "^| " .planning/phases/01-audit-and-harden-scaffold/AUDIT.md` — at least 34 table rows in the main matrix.
    For each ID in the frontmatter `requirements:` list, `grep "| {ID} |" AUDIT.md` returns exactly one row.
    `grep -n "Status:.*GREEN" AUDIT.md` — non-empty (unless Task 1 detected a failure, in which case AMBER is correct and the plan should have halted).
    `grep -n "Gate decision" AUDIT.md` — non-empty.
  </verify>
  <done>
    `AUDIT.md` exists with a complete 34-row requirement-to-evidence matrix. Status reflects the true pytest state. Gate decision section tells Phase 2 whether to proceed.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/ -q` — green (0 failed, 0 errored)
- `test -f .planning/phases/01-audit-and-harden-scaffold/AUDIT.md`
- Every Phase 1 requirement ID (34 total) appears exactly once in AUDIT.md's main matrix
- AUDIT.md Status is GREEN (or AMBER with blocking issues enumerated)
- `grep "\\.lower()" src/harness_eng/grader.py` — empty (hardening invariant holds)
- `grep "max_retries=0" src/harness_eng/model.py` — non-empty (hardening invariant holds)
- `grep "SCHEMA_VERSION" src/harness_eng/trace.py` — non-empty (hardening invariant holds)
- `test -f HELD_OUT.md` — exists
</verification>

<success_criteria>
1. Full pytest suite is green at the audit commit (no failures, no errors, no unexpected skips).
2. `AUDIT.md` exists under `.planning/phases/01-audit-and-harden-scaffold/` with the 34-row requirement-to-evidence matrix.
3. Every Phase 1 requirement ID in this plan's frontmatter appears exactly once in the matrix with concrete evidence.
4. Structural invariant grep evidence table populated with actual grep results.
5. Gate decision section explicitly says GREEN (safe for Phase 2) or AMBER with blocker enumeration.
6. No source file (src/ or tests/) modified by this plan — it is a documentation-only audit.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-and-harden-scaffold/01-05-SUMMARY.md` summarizing:
- Gate decision (GREEN / AMBER)
- Test pass count (total and delta from pre-phase 19)
- Any requirement IDs that could not be pinned to evidence (should be zero for GREEN)
- Handoff to Phase 2: "safe to tag harnesses-frozen at commit <SHA>" OR "blocked until <specific issue>"
</output>
