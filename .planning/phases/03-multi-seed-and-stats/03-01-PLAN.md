---
phase: 03-multi-seed-and-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/harness_eng/runner.py
  - src/harness_eng/analysis.py
  - scripts/run_full.py
  - scripts/run_pilot.py
  - tests/test_stats.py
autonomous: true
requirements:
  - RUN-02
  - ANAL-02
  - ANAL-05
---

<objective>
Make the published numbers statistically honest. After this plan lands:
- Seeds default to 3, required via CLI arg --seeds N
- analysis.py computes Wilson 95% CI on binomial success rate per harness
- frontier.png renders points with vertical error bars from the Wilson interval
- summary.csv includes cost_per_success_usd column (total_cost / n_successes)
- A new test pins the Wilson math to a known reference value (1e-6 tolerance)

All edits are to non-gated files (runner.py, analysis.py, scripts/). Freeze gate stays green.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Wilson CI helper + integrate into analysis.py</name>
  <files>src/harness_eng/analysis.py</files>
  <action>
    Add `_wilson_ci(successes, trials, z=1.96) -> tuple[float, float]` returning (low, high). Include it in per-harness aggregation: add `ci_low` and `ci_high` columns to df_harness. Add `cost_per_success_usd` column (total_cost / n_successes, NaN if zero successes).

    Update frontier_chart to draw vertical error bars via matplotlib's yerr=[[y - ci_low], [ci_high - y]] per point.
  </action>
</task>

<task type="auto">
  <name>Task 2: Seed support in runner + CLI defaults</name>
  <files>src/harness_eng/runner.py, scripts/run_full.py, scripts/run_pilot.py</files>
  <action>
    Runner already accepts `seeds: int = 1` — bump default to 3. `scripts/run_full.py` and `run_pilot.py` default `--seeds 3`. Document the choice in docstrings.
  </action>
</task>

<task type="auto">
  <name>Task 3: Test Wilson implementation against reference</name>
  <files>tests/test_stats.py</files>
  <action>
    Wilson 95% CI for 7 successes / 10 trials at z=1.96 is approximately (0.39665, 0.89084). Test asserts within 1e-4. Also test: 0/10 lower ≈ 0 (no negative), 10/10 upper ≈ 1, point-estimate order matches standard proportion ordering.
  </action>
</task>

</tasks>

<success_criteria>
1. `_wilson_ci(7, 10)` returns (~0.3966, ~0.8908).
2. summary.csv has ci_low, ci_high, cost_per_success_usd columns.
3. frontier.png has error bars (verified by opening after a synthetic run, or by asserting no matplotlib exception).
4. Default seeds is 3 in runner + scripts.
5. Full pytest suite green.
6. `git diff harnesses-frozen HEAD -- src/harness_eng/harnesses/ src/harness_eng/tools.py src/harness_eng/model.py` is empty (non-gated files only).
</success_criteria>
