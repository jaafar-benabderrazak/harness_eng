---
phase: 07-ci-and-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .gitattributes
  - README.md
autonomous: true
requirements:
  - BENCH-05
  - TEST-03
  - ONB-01
---

<objective>
Final polish:
1. Expand CI to run pytest on Linux AND Windows (grader determinism check must pass identically across OSes)
2. Add `.gitattributes` pinning HTML fixtures and JSONL artifacts as binary so line-ending conversion doesn't mangle reproducibility
3. README quickstart section mirrors the CI command sequence verbatim

Non-gated files only. Freeze gate stays clean.
</objective>

<tasks>
<task type="auto"><name>Task 1: CI matrix</name><files>.github/workflows/ci.yml</files><action>Switch the workflow to a matrix strategy over [ubuntu-latest, windows-latest]. Keep python-version 3.11. The grader determinism test is the load-bearing one here — its green on Windows proves the NFC/casefold pipeline is OS-independent.</action></task>
<task type="auto"><name>Task 2: .gitattributes</name><files>.gitattributes</files><action>Pin `*.html` and `*.jsonl` as binary (`-text`) so CRLF conversion never mangles fixture bytes or trace bytes. Explicitly set `*.py text eol=lf` to prevent CRLF creep in source.</action></task>
<task type="auto"><name>Task 3: README quickstart align to CI</name><files>README.md</files><action>Ensure the Quickstart's command sequence is exactly what CI runs (minus secrets). Add a note that API key is only needed for the full matrix, not the test suite.</action></task>
</tasks>

<success_criteria>
1. CI workflow runs on ubuntu-latest AND windows-latest
2. `.gitattributes` exists and pins html/jsonl as binary
3. README quickstart is copy-pasteable to match CI
4. `git diff harnesses-frozen HEAD -- <gated>` remains empty
</success_criteria>
